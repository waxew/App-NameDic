# Finalization Plan — App-NameDic 2.0.0

این سند محدودهٔ نسخهٔ تکمیل‌شدهٔ 2.0.0 را مشخص می‌کند تا «پروژه کامل» به یک خروجی قابل آزمون و قابل تحویل تبدیل شود.

## دامنهٔ نهایی

- انتخاب نام: دختر/پسر، جستجو، کشف، پسندیده‌ها و مقایسه.
- پیشنهادگر پیشرفتهٔ آفلاین: جنسیت، حرف اول، عبارت معنی/ریشه، فرهنگ و حداقل غنای داده.
- تاریخ ایران: شخصیت‌های تاریخی، سلسله‌ها و Timeline خلاصه.
- آزمون ایران‌شناسی آفلاین بر پایهٔ داده‌های خود برنامه.
- اشتراک‌گذاری اطلاعات یک نام با Android Sharesheet.
- گزارش خطای داده با ایمیل آماده‌شده و شناسهٔ رکورد.
- صفحهٔ «درباره داده‌ها» با شفافیت دربارهٔ منبع و نبود داده.
- اعتبارسنجی داده‌های نام، تاریخ و سلسله‌ها در CI.
- lint + unit tests + debug build در CI.
- نسخه‌بندی و updater هماهنگ.
- حفظ applicationId و سازگاری به‌روزرسانی.

## خارج از ادعای 2.0.0

- نقشهٔ جغرافیایی دقیق قومیت/زبان بدون dataset مکانی منبع‌دار نمایش داده نمی‌شود. به‌جای حدس مرزهای فرهنگی، UI فقط دسته‌های دارای داده را نشان می‌دهد.
- Release امضاشده فقط با keystore اصلی پروژه ساخته می‌شود؛ ساخت کلید جدید برای نسخهٔ نهایی ممنوع است چون مسیر update را می‌شکند.
- دادهٔ تاریخی خلاصه است و جایگزین منبع دانشگاهی نیست؛ لینک منبع برای مطالعهٔ بیشتر نگه داشته می‌شود.
